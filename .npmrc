enable-pre-post-scripts=true
# eslint-config-next pulls its plugins in transitively, and ESLint resolves
# plugins from the project root — pnpm's strict linking hides them without this.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*@typescript-eslint*
